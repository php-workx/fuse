#!/usr/bin/env bash
set -euo pipefail

# Pre-push: local quality gate excluding SonarQube.
# Install: just install-hooks

export PATH="/opt/homebrew/bin:/usr/local/bin:$HOME/go/bin:$HOME/.cargo/bin:$PATH"

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

exec just check-local
