#!/usr/bin/env bash
set -euo pipefail

# Pre-commit: fast quality checks. Delegates to justfile.
# Install: just install-dev

export PATH="/opt/homebrew/bin:/usr/local/bin:$HOME/go/bin:$HOME/.cargo/bin:$PATH"

cd "$(git rev-parse --show-toplevel)"
just pre-commit
