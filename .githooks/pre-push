#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

if command -v pre-commit >/dev/null 2>&1 && [ -f .pre-commit-config.yaml ]; then
  exec pre-commit run --hook-stage pre-push --all-files
fi

exec bash ./scripts/check-pre-push.sh
